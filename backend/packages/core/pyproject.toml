[project]
name = "onboarding-core"
version = "0.1.0"
description = "Onboarding domain: entities, eligibility and pricing rules, and the ports adapters implement"
requires-python = ">=3.13,<3.14"
dependencies = [
    "pycryptodome>=3.21",
]

[build-system]
requires = ["uv_build>=0.9,<0.10"]
build-backend = "uv_build"
